<%= link_to("<< Back to List", {action: 'list'}, :class => 'back-link') %>

<div class="type new">
  <h2>Create Type</h2>

  <%= form_for(:type, :url => {:action => 'create'}) do |f| %>

    <table summary="Type form fields">
      <tr>
        <th>Name</th>
        <td><%= f.text_field(:name) %></td>
      </tr>
      <tr>
        <th>Image</th>
        <td>
          <%= f.fields_for :image do |i| %>
            <%= i.file_field(:image) %>
          <% end %>
        </td>
      </tr>
   </table>

    <div class="form-buttons">
      <%= submit_tag("Add Type") %>
    </div>

  <% end %>
</div>

