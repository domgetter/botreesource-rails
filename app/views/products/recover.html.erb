<%= link_to("<< Back to list", {action: 'list'}, class: 'back-link') %>

  <div class="product recover">
    <h1>Recover Product</h1>

  <%= form_for(:product, url: {action: 'restore', id: @product.id}) do |f| %>

    <p>Are you sure you want to recover this Product?</p>

    <p class="reference-name"><%= @product.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Recover Product") %>
    </div>

  <% end %>

  </div>

