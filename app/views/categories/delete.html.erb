
<%= link_to("<< Back to list", {action: 'list'}, class: 'back-link') %>

  <div class="category delete">
    <h1>Delete Category</h1>

  <%= form_for(:category, url: {action: 'destroy', id: @category.id}) do |f| %>

    <p>Are you sure you want to delete this Category?  Doing so will orphan (but not delete) the Products contained within.</p>

    <p class="reference-name"><%= @category.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Category") %>
    </div>

  <% end %>

  </div>

